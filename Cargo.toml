[workspace]
members = ["lib", "demos", "uci", "tui"]
resolver = "2"

[workspace.package]
version = "1.1.0"
edition = "2024"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "symbols"
